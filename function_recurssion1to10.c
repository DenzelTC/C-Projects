#include <stdio.h>
int number(int i){
	int num=i;
	if (i=10)
		return ;//base statement 
	else
 	printf ("i+1",i);
	  number(i++);
	
	getch();
	}
void main(){
	int i;
	
  number(i);
  printf("%d",number(10));
  
  getch();
}
