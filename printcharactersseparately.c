#include <stdio.h>
 
int main()
{
   	char str[80];
   	char i;
   	printf("\t\t\t Returns characters separately");
	scanf("%s",&str[i]);
   	for(i=0;i<10;i++){
   		printf("%c\n", str[i]);
	   }
 
   	getch();
}
