#include <stdio.h>
void main(){
	int a,b,c;
	printf("			Triangle type idenfifier\n");
	printf("\nEnter length of 1st side:....");
	scanf("%d",&a);
	printf("\nEnter length of 2nd side:....");
	scanf("%d",&b);
	printf("\nEnter length of 3rd side:....");
	scanf("%d",&c);
	if ((a==b)or(a==c))
		{
			printf("This triangle is an iscoseles triangle");
		}
	if (a==b)
	{if (b==c){
		printf("This triangle is an equilateral triangle");
	}
	

	if (a>b)
	getch();
}
